{
  "name": "@pulseboard/shared-db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "push": "drizzle-kit push",
    "studio": "drizzle-kit studio",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist drizzle *.tsbuildinfo"
  },
  "dependencies": {
    "drizzle-orm": "^0.40.0",
    "postgres": "^3.4.5",
    "ulidx": "^2.4.0",
    "@pulseboard/shared-types": "workspace:*"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.0",
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0",
    "dotenv": "^16.4.0"
  }
}
